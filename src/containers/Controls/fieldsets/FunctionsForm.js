const FunctionsForm = ({ functions }) => {
  return (
    <form>
      <fieldset>
        <legend>Functions</legend>
        <button>Add Function</button>
      </fieldset>
    </form>
  );
};

export default FunctionsForm;
